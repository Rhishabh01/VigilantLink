version: '3.8'

services:
  vigilantlink-backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: vigilantlink-backend
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
    volumes:
      - ./backend:/app
    # IPC host is recommended for Playwright to avoid shared memory issues
    ipc: host
